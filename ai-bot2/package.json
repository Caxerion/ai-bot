{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && esbuild index.js --bundle --platform=node --target=node18 --format=cjs --outfile=dist/index.js --minify",
    "start": "node dist/index.js",
    "dev": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.20.0",
    "@google/generative-ai": "^0.21.0",
    "body-parser": "^1.20.3",
    "esbuild": "^0.25.10",
    "express": "^4.21.2",
    "handlebars": "^4.7.8",
    "node-fetch": "^3.3.2",
    "rimraf": "^6.0.1"
  }
}
